<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="arraysort.project.board.app.user.mapper.UserMapper">

    <!-- 회원가입 -->
    <insert id="insertUser" parameterType="user.domain.UserVO">
        insert into user(user_id, user_password, user_name)
        values (#{userId}, #{userPassword}, #{userName})
    </insert>

    <!-- 아이디 중복 확인을 위한 카운트-->
    <select id="selectUserCountByUserId" parameterType="String" resultType="int">
        select count(*)
        from user
        where user_id = #{userId}
    </select>

</mapper>
